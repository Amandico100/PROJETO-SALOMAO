# PROMPT MESTRE

# 📋 MICRO-TAREFAS DO SALOMÃO V1
## PARTE 7: FASE PROMPT MESTRE (O Tradutor Final)
### Formato Híbrido Executável — Versão 100/100

---

# 🎯 A FASE ONDE TUDO VIRA CÓDIGO EXECUTÁVEL

> **"O Prompt Mestre não é só um documento. É a RECEITA PERFEITA que transforma toda a inteligência das 6 fases anteriores em um aplicativo funcional que o builder constrói SEM PRECISAR PERGUNTAR NADA."**

Esta é a fase onde o Salomão atua como **Tradutor Final** — pegando tudo que foi decidido (Discovery → Psychology → Ideation → Branding → Oferta → Engenharia) e transformando em um **prompt executável de altíssima precisão**.

---

# 🧠 FILOSOFIA FUNDAMENTAL DESTA FASE

```
┌─────────────────────────────────────────────────────────────────────┐
│            PROMPT MESTRE = TRADUÇÃO PERFEITA                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ O builder (Lovable/Replit) é BURRO.                                │
│ Ele faz EXATAMENTE o que você pede.                                │
│ Se você pede errado, ele constrói errado.                          │
│ Se você pede certo, ele constrói PERFEITO.                         │
│                                                                     │
│ PROMPT MESTRE NÃO É:                                                │
│ ❌ "Crie um app de calculadora bonito"                             │
│ ❌ Descrição vaga com "use sua criatividade"                       │
│ ❌ Documento que deixa decisões para o builder                     │
│ ❌ Texto que precisa de interpretação                              │
│                                                                     │
│ PROMPT MESTRE É:                                                    │
│ ✅ Especificação EXATA de cada tela                                │
│ ✅ Código/pseudocódigo da lógica completa                          │
│ ✅ Copy LITERAL de cada elemento                                   │
│ ✅ Cores em HEX, fontes por nome, tamanhos em px                   │
│ ✅ Fluxo de navegação SEM ambiguidade                              │
│ ✅ Integrações com endpoints e payloads REAIS                      │
│ ✅ Variáveis de ambiente NOMEADAS                                  │
│ ✅ Lista de "NÃO FAZER" para evitar invenções                      │
│                                                                     │
│ REGRA DE OURO:                                                      │
│ "Se o builder precisar tomar UMA decisão sozinho,                  │
│  o Prompt Mestre está INCOMPLETO."                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

# VISÃO GERAL DA FASE

| Atributo | Valor |
|----------|-------|
| **Nome da Fase** | PROMPT MESTRE |
| **Número** | Fase 7 de 8 |
| **Apelido** | "O Tradutor Final" |
| **Objetivo** | Transformar blueprint em prompt 100% executável |
| **Duração Típica** | 1-2 mensagens (principalmente automático) |
| **Entradas** | Blueprint + Mapa de Telas + Lógica + Integrações (da Fase 6) |
| **Saídas** | Prompt Mestre + Instruções Cliente + Checklist Pós-Build |
| **Micro-tarefas** | 58 (organizadas em 8 blocos) |

---

# O QUE ENTRA E O QUE SAI

```
┌─────────────────────────────────────────────────────────────────────┐
│                      ENTRADAS (da Fase 6)                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ DO BLUEPRINT TÉCNICO:                                              │
│ • Mapa completo de telas (IDs, tipos, ordem)                       │
│ • Especificação de cada elemento                                   │
│ • Fluxo de dados entre telas                                       │
│ • Validações definidas                                              │
│                                                                     │
│ DA LÓGICA:                                                          │
│ • Função de cálculo/resultado especificada                         │
│ • Variáveis mapeadas                                                │
│ • Formatação do resultado                                           │
│                                                                     │
│ DAS INTEGRAÇÕES:                                                    │
│ • Webhooks definidos (5 pontos)                                    │
│ • IDs únicos de cada elemento                                      │
│ • Armazenamento escolhido (Supabase/Sheets/etc.)                   │
│ • Modo de imagem (0/1/2/3) — SE APLICÁVEL                          │
│ • API de imagem escolhida (SE modo 1)                              │
│ • Passo a passo de configuração da API                             │
│                                                                     │
│ DA COPY (via Branding):                                             │
│ • Headlines de cada tela                                           │
│ • Textos de perguntas                                              │
│ • Copy do resultado                                                 │
│ • Texto do CTA                                                      │
│ • Tom de voz                                                        │
│                                                                     │
│ DA OFERTA:                                                          │
│ • Destino do CTA (WhatsApp, página, etc.)                          │
│ • Mensagem pré-preenchida                                          │
│ • Número de WhatsApp                                               │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                      SAÍDAS (desta fase)                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ 1. PROMPT MESTRE COMPLETO                                          │
│    • Documento único, pronto para copiar/colar                     │
│    • Zero ambiguidade                                              │
│    • Testado mentalmente contra erros comuns                       │
│                                                                     │
│ 2. ARQUIVO DE INSTRUÇÕES PARA O CLIENTE                            │
│    • Passo a passo para criar contas (se necessário)               │
│    • Como pegar chaves de API                                      │
│    • Onde colar cada variável de ambiente                          │
│    • Em linguagem 100% leiga                                       │
│                                                                     │
│ 3. CHECKLIST DE VERIFICAÇÃO PÓS-BUILD                              │
│    • O que testar depois que o app for criado                      │
│    • Erros comuns e como resolver                                  │
│    • Critérios de "está pronto"                                    │
│                                                                     │
│ 4. VARIÁVEIS DE AMBIENTE MAPEADAS                                  │
│    • Lista completa com nomes padronizados                         │
│    • Qual variável serve para quê                                  │
│    • Valores de exemplo                                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

# MAPA DOS BLOCOS

```
┌─────────────────────────────────────────────────────────────────────┐
│                      FASE 7: PROMPT MESTRE                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  BLOCO 7.1              BLOCO 7.2              BLOCO 7.3           │
│  ┌────────────────┐     ┌────────────────┐     ┌────────────────┐  │
│  │ CARREGAR E     │  →  │ MONTAR SEÇÃO   │  →  │ MONTAR SEÇÃO   │  │
│  │ CONSOLIDAR     │     │ VISÃO GERAL    │     │ TELAS          │  │
│  │ TODAS ENTRADAS │     │ DO PROMPT      │     │ (DETALHADA)    │  │
│  │ (6 tarefas)    │     │ (6 tarefas)    │     │ (10 tarefas)   │  │
│  └────────────────┘     └────────────────┘     └────────────────┘  │
│         │                                              │           │
│         ▼                                              ▼           │
│  BLOCO 7.4              BLOCO 7.5              BLOCO 7.6           │
│  ┌────────────────┐     ┌────────────────┐     ┌────────────────┐  │
│  │ MONTAR SEÇÃO   │  →  │ MONTAR SEÇÃO   │  →  │ MONTAR SEÇÃO   │  │
│  │ LÓGICA DE      │     │ INTEGRAÇÕES    │     │ ESTILO E       │  │
│  │ NEGÓCIO        │     │ E APIs         │     │ CONFIGURAÇÕES  │  │
│  │ (8 tarefas)    │     │ (10 tarefas)   │     │ (6 tarefas)    │  │
│  └────────────────┘     └────────────────┘     └────────────────┘  │
│                                                        │           │
│                                                        ▼           │
│                    BLOCO 7.7              BLOCO 7.8                 │
│                    ┌────────────────┐     ┌────────────────┐       │
│                    │ VALIDAR E      │  →  │ GERAR          │       │
│                    │ BLINDAR        │     │ MATERIAIS      │       │
│                    │ PROMPT         │     │ COMPLEMENTARES │       │
│                    │ (6 tarefas)    │     │ (6 tarefas)    │       │
│                    └────────────────┘     └────────────────┘       │
│                                                   │                │
│                                                   ▼                │
│                              → FASE 8: MARKETING                   │
└─────────────────────────────────────────────────────────────────────┘
```

---

# 🔧 PRINCÍPIOS CRÍTICOS DESTA FASE

## PRINCÍPIO 1: CONSUMIR, NÃO CRIAR

```
┌─────────────────────────────────────────────────────────────────────┐
│ A FASE 7 NÃO CRIA NOVAS DECISÕES — ELA TRADUZ                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ ❌ ERRADO:                                                         │
│ "Vou adicionar uma nova integração com API X"                      │
│ "Acho que essa tela ficaria melhor com 3 botões"                   │
│ "Vou mudar a lógica para ser mais interessante"                    │
│                                                                     │
│ ✅ CERTO:                                                          │
│ "A Fase 6 definiu modo_imagem = 1, então incluo a API"            │
│ "O Branding definiu 2 opções no CTA, mantenho"                    │
│ "A lógica está no Blueprint, apenas traduzo para código"          │
│                                                                     │
│ REGRA:                                                              │
│ Se não foi decidido nas fases 1-6, NÃO decide aqui.               │
│ Se precisar decidir algo, VOLTA para a fase certa.                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## PRINCÍPIO 2: RESPEITAR MODOS DE IMAGEM

```
┌─────────────────────────────────────────────────────────────────────┐
│ USAR EXATAMENTE O QUE A FASE 6 DEFINIU                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ SE modo_imagem = 0:                                                │
│ • Não incluir código de geração de imagem                         │
│ • Não prometer features visuais no prompt                         │
│ • Usar apenas imagens estáticas se precisar                       │
│                                                                     │
│ SE modo_imagem = 1 (API one-shot):                                 │
│ • Incluir seção de integração com API                             │
│ • Usar endpoint e payload definidos na Fase 6                     │
│ • Incluir variável de ambiente para API key                       │
│ • Incluir fallback para erro de API                               │
│                                                                     │
│ SE modo_imagem = 2 (Curadoria):                                    │
│ • Incluir lógica de seleção de imagem                             │
│ • Referenciar banco de imagens definido                           │
│ • Sem chamadas de API externas                                    │
│                                                                     │
│ SE modo_imagem = 3 (PRO/Futuro):                                   │
│ • NÃO IMPLEMENTAR na V1                                           │
│ • Apenas documentar como "módulo avançado"                        │
│ • Usar fallback do modo 0 ou 2                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## PRINCÍPIO 3: ZERO AMBIGUIDADE

```
┌─────────────────────────────────────────────────────────────────────┐
│ CHECKLIST DE CLAREZA — CADA ELEMENTO DEVE TER:                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ TEXTOS:                                                            │
│ □ Texto LITERAL entre aspas (não "algo como...")                  │
│ □ Variáveis marcadas com {nome_variavel}                          │
│ □ Quebras de linha explícitas onde necessário                     │
│                                                                     │
│ CORES:                                                              │
│ □ Em formato HEX (#RRGGBB)                                        │
│ □ Nunca "azul" ou "verde claro"                                   │
│ □ Com fallback se necessário                                      │
│                                                                     │
│ TAMANHOS:                                                           │
│ □ Em pixels ou unidades relativas (px, rem, %)                    │
│ □ Nunca "grande" ou "médio"                                       │
│                                                                     │
│ COMPORTAMENTOS:                                                     │
│ □ "Ao clicar no botão X, vai para tela Y"                        │
│ □ "Se campo vazio, mostrar erro: 'Texto do erro'"                │
│ □ "Animação de fade-in de 300ms"                                 │
│                                                                     │
│ DADOS:                                                              │
│ □ Tipo de cada variável (string, number, boolean)                 │
│ □ Formato esperado ("DD/MM/AAAA", "00.000,00")                    │
│ □ Valores de exemplo                                              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

# BLOCO 7.1: CARREGAR E CONSOLIDAR TODAS AS ENTRADAS

**Objetivo:** Reunir TUDO das fases anteriores em um único contexto.

---

## 7.1.1 — CARREGAR BLUEPRINT TÉCNICO DA FASE 6

```
[AUTOMÁTICO] Importar blueprint completo

MAPA DE TELAS:
□ lista_telas: [array com todas as telas em ordem]
□ para cada tela:
  ├── id: [identificador único]
  ├── tipo: [ENTRADA | PERGUNTA | CAPTURA | LOADING | RESULTADO | etc.]
  ├── ordem: [número sequencial]
  └── elementos: [lista de componentes]

FLUXO DE NAVEGAÇÃO:
□ transicoes: [tela_origem → tela_destino para cada ação]
□ condicoes: [se houver lógica de branching]

ARQUITETURA:
□ framework: [React/Vue/vanilla]
□ estrutura_pastas: [se definida]
□ gerenciamento_estado: [como dados fluem]

🚦 AVANÇA QUANDO: Blueprint carregado
```

---

## 7.1.2 — CARREGAR ESPECIFICAÇÕES DE LÓGICA

```
[AUTOMÁTICO] Importar toda a lógica de negócio

VARIÁVEIS:
□ variaveis_coletadas: [lista de inputs do usuário]
□ variaveis_calculadas: [lista de outputs do cálculo]
□ variaveis_sistema: [timestamp, session_id, etc.]

LÓGICA DE CÁLCULO:
□ tipo_logica: [fórmula | tabela lookup | regras if/else | API]
□ codigo_ou_pseudocodigo: [a função completa]
□ casos_de_teste: [exemplos de input → output]

FORMATAÇÃO:
□ formato_resultado_principal: [moeda | porcentagem | texto | etc.]
□ prefixo_sufixo: [R$, %, "dias", etc.]
□ casas_decimais: [se aplicável]

🚦 AVANÇA QUANDO: Lógica carregada
```

---

## 7.1.3 — CARREGAR INTEGRAÇÕES E APIs

```
[AUTOMÁTICO] Importar todas as integrações definidas

ARMAZENAMENTO:
□ tipo: [Supabase | Google Sheets | webhook CRM | local storage]
□ estrutura_dados: [schema da tabela/planilha]
□ credenciais_necessarias: [quais variáveis de ambiente]

WEBHOOKS:
□ webhook_inicio_sessao: [trigger, endpoint, payload]
□ webhook_lead_capturado: [trigger, endpoint, payload]
□ webhook_resultado_gerado: [trigger, endpoint, payload]
□ webhook_cta_clicado: [trigger, endpoint, payload]
□ webhook_abandono: [trigger, endpoint, payload]

IMAGEM (SE APLICÁVEL):
□ modo_imagem: [0 | 1 | 2 | 3]
□ SE modo = 1:
  ├── api_escolhida: [nome da API]
  ├── endpoint: [URL completa]
  ├── metodo: [POST/GET]
  ├── payload: [estrutura]
  ├── autenticacao: [Bearer, API-Key, etc.]
  └── variavel_ambiente: [nome da env var]
□ SE modo = 2:
  ├── origem_imagens: [cliente | banco genérico]
  └── regra_selecao: [como escolher qual imagem]

CTA:
□ tipo_cta: [WhatsApp | página | agendamento | formulário]
□ destino: [URL ou número]
□ mensagem_preenchida: [texto com variáveis]

🚦 AVANÇA QUANDO: Integrações carregadas
```

---

## 7.1.4 — CARREGAR COPY COMPLETA

```
[AUTOMÁTICO] Importar todos os textos do Branding

PARA CADA TELA, CARREGAR:
□ headline: [texto exato]
□ subheadline: [se houver]
□ textos_auxiliares: [instruções, dicas, etc.]
□ labels_de_campos: [nome do campo]
□ placeholders: [texto dentro do campo]
□ textos_de_botoes: [CTA de cada botão]
□ mensagens_de_erro: [validações]
□ textos_de_loading: [se houver tela de loading]

TOM DE VOZ:
□ persona: [formal | amigável | técnico | etc.]
□ tratamento: [você | tu | senhor/senhora]

🚦 AVANÇA QUANDO: Copy carregada
```

---

## 7.1.5 — CARREGAR DADOS DA OFERTA

```
[AUTOMÁTICO] Importar estrutura da oferta

OFERTA:
□ nome_metodo: [nome do programa/método]
□ promessa: [resultado + prazo]
□ garantia: [tipo e condições]
□ preco: [se exibido no app]

CTA FINAL:
□ texto_botao_principal: [texto exato]
□ texto_reforco: [abaixo do botão]
□ urgencia: [se houver]

WHATSAPP (se aplicável):
□ numero: [com código do país]
□ mensagem_template: [texto com {variaveis}]

🚦 AVANÇA QUANDO: Oferta carregada
```

---

## 7.1.6 — CONSOLIDAR CONTEXTO COMPLETO

```
[REGISTRO] Criar visão unificada de tudo

RESUMO DO APP:
├── nome_isca: [...]
├── tipo_isca: [calculadora | diagnóstico | quiz | etc.]
├── numero_telas: [X]
├── usa_imagem: [sim/não + modo]
├── usa_api_externa: [sim/não + qual]
├── armazenamento: [tipo]
└── cta_destino: [WhatsApp/página/etc.]

COMPLEXIDADE:
├── nivel: [baixa | média | alta]
├── integrações: [quantidade]
└── estimativa_build: [tempo aproximado]

🚦 AVANÇA QUANDO: Contexto consolidado e pronto
```

---

# BLOCO 7.2: MONTAR SEÇÃO VISÃO GERAL DO PROMPT

**Objetivo:** Criar a introdução do prompt que orienta o builder.

---

## 7.2.1 — GERAR CABEÇALHO DO PROMPT

```
[AUTOMÁTICO] Criar cabeçalho padronizado

┌─────────────────────────────────────────────────────────────────────┐
│ PROMPT MESTRE — [NOME DA ISCA]                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ GERADO POR: Salomão.AI                                             │
│ VERSÃO: 1.0                                                        │
│ DATA: [data de geração]                                            │
│ CLIENTE: [nome do negócio]                                         │
│ NICHO: [área de atuação]                                           │
│                                                                     │
│ ⚠️ INSTRUÇÕES PARA O BUILDER:                                      │
│ Este prompt contém TODAS as especificações necessárias.            │
│ Siga EXATAMENTE o que está escrito.                                │
│ NÃO adicione features não especificadas.                           │
│ NÃO altere textos, cores ou comportamentos.                        │
│ Se algo parecer estranho, mantenha como está.                      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

🚦 AVANÇA QUANDO: Cabeçalho gerado
```

---

## 7.2.2 — GERAR DESCRIÇÃO DO PROJETO

```
[AUTOMÁTICO] Descrever o que o app faz

## VISÃO GERAL DO PROJETO

### O que é
[NOME_DA_ISCA] é um aplicativo web responsivo que funciona como 
uma [TIPO: calculadora/diagnóstico/quiz/simulador] para [NICHO].

### Objetivo
[Descrição clara do que o app faz e qual resultado entrega]

### Público-alvo
[Quem vai usar o app - descrição do usuário final]

### Resultado esperado
O usuário responde [X] perguntas e recebe [DESCRIÇÃO DO RESULTADO],
que o motiva a [AÇÃO DO CTA].

🚦 AVANÇA QUANDO: Descrição gerada
```

---

## 7.2.3 — GERAR ESPECIFICAÇÕES TÉCNICAS GERAIS

```
[AUTOMÁTICO] Definir stack e requisitos

## ESPECIFICAÇÕES TÉCNICAS

### Stack recomendada
- **Framework:** React + Vite (ou Next.js)
- **Estilo:** Tailwind CSS
- **Ícones:** Lucide React
- **Responsividade:** Mobile-first

### Requisitos obrigatórios
- [ ] Funcionar em dispositivos móveis (320px+)
- [ ] Funcionar em desktop (1024px+)
- [ ] Navegação sem refresh de página (SPA)
- [ ] Transições suaves entre telas (300ms fade)
- [ ] Loading states em todas as ações
- [ ] Tratamento de erros amigável

### Estrutura de pastas sugerida
```
/src
  /components      # Componentes reutilizáveis
  /screens         # Telas do app
  /hooks           # Custom hooks
  /utils           # Funções auxiliares
  /styles          # Estilos globais
  /assets          # Imagens e ícones
```

🚦 AVANÇA QUANDO: Especificações geradas
```

---

## 7.2.4 — GERAR MAPA DE NAVEGAÇÃO

```
[AUTOMÁTICO] Criar diagrama de fluxo em texto

## MAPA DE NAVEGAÇÃO

```
[TELA 1: ENTRADA]
      │
      ▼ (clique em "Começar")
[TELA 2: PERGUNTA 1]
      │
      ▼ (resposta selecionada)
[TELA 3: PERGUNTA 2]
      │
      ▼ (resposta selecionada)
      ...
      │
      ▼
[TELA N: CAPTURA] (se houver)
      │
      ▼ (dados enviados)
[TELA N+1: LOADING]
      │
      ▼ (auto, após 4s)
[TELA N+2: RESULTADO]
      │
      ▼ (clique em "Continuar")
[TELA N+3: CTA]
      │
      ▼ (clique no CTA)
[AÇÃO: WhatsApp/Página/etc.]
```

**Total de telas:** [X]
**Tempo estimado do usuário:** [Y] minutos

🚦 AVANÇA QUANDO: Mapa gerado
```

---

## 7.2.5 — GERAR LISTA DE VARIÁVEIS DE AMBIENTE

```
[AUTOMÁTICO] Listar todas as env vars necessárias

## VARIÁVEIS DE AMBIENTE

O cliente precisa configurar as seguintes variáveis:

### Obrigatórias
```env
# Dados do negócio
WHATSAPP_NUMERO=5511999999999
NOME_EMPRESA="Nome da Empresa"
```

### Condicionais (se usar Supabase)
```env
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJxx...
```

### Condicionais (se usar API de imagem)
```env
[NOME_API]_API_KEY=sua_chave_aqui
```

### Opcionais (webhooks)
```env
WEBHOOK_LEAD_URL=https://...
WEBHOOK_RESULTADO_URL=https://...
```

**Nota:** O arquivo INSTRUCOES_CLIENTE.md explica como obter cada chave.

🚦 AVANÇA QUANDO: Variáveis listadas
```

---

## 7.2.6 — CONSOLIDAR SEÇÃO VISÃO GERAL

```
[REGISTRO] Juntar tudo da visão geral

SEÇÃO 1 DO PROMPT MESTRE:
├── Cabeçalho com instruções
├── Descrição do projeto
├── Especificações técnicas
├── Mapa de navegação
└── Lista de variáveis de ambiente

🚦 AVANÇA QUANDO: Seção 1 consolidada
```

---

# BLOCO 7.3: MONTAR SEÇÃO DE TELAS (DETALHADA)

**Objetivo:** Especificar cada tela com precisão absoluta.

📦 **MATERIAIS:** 
- Mapa de telas da Fase 6
- Copy do Branding

---

## 7.3.1 — GERAR TEMPLATE DE ESPECIFICAÇÃO DE TELA

```
[AUTOMÁTICO] Criar estrutura padrão para cada tela

### TELA [N]: [NOME_DA_TELA]

**ID:** `tela_[nome]_[numero]`
**Tipo:** [ENTRADA | PERGUNTA | CAPTURA | LOADING | RESULTADO | CTA | etc.]
**Arquivo:** `/src/screens/[NomeTela].jsx`

#### Layout (wireframe em texto)
```
┌─────────────────────────────────────┐
│ [LOGO ou ÍCONE] (se houver)        │
│                                     │
│ [HEADLINE]                          │
│ [Subheadline] (se houver)          │
│                                     │
│ [ELEMENTO PRINCIPAL]                │
│ (input, opções, resultado, etc.)   │
│                                     │
│ [BOTÃO CTA]                         │
│ [texto auxiliar] (se houver)       │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| headline | h1 | "Texto exato" | text-2xl font-bold text-[#HEX] |
| ... | ... | ... | ... |

#### Dados

- **Input:** [o que esta tela recebe]
- **Output:** [o que esta tela produz/salva]
- **Variável salva:** `nome_variavel`

#### Comportamentos

- Ao clicar em [X]: [ação]
- Se [condição]: [comportamento]
- Validação: [regra]

#### Navegação

- **Vem de:** Tela [N-1]
- **Vai para:** Tela [N+1]
- **Condição:** [se houver branching]

🚦 AVANÇA QUANDO: Template definido
```

---

## 7.3.2 — GERAR ESPECIFICAÇÃO: TELA DE ENTRADA

```
[AUTOMÁTICO] Detalhar tela de entrada

### TELA 1: ENTRADA

**ID:** `tela_entrada_001`
**Tipo:** ENTRADA
**Arquivo:** `/src/screens/TelaEntrada.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│         [LOGO] (opcional)          │
│                                     │
│    [HEADLINE PRINCIPAL]             │
│    [Subheadline de apoio]          │
│                                     │
│    [Prova social rápida]           │
│    (ex: "Já ajudamos +500...")     │
│                                     │
│    ┌─────────────────────────┐     │
│    │   [BOTÃO COMEÇAR]       │     │
│    └─────────────────────────┘     │
│                                     │
│    [Texto de confiança]            │
│    (ex: "Leva menos de 2 min")    │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| logo | img | `/assets/logo.png` | w-20 h-20 mx-auto mb-4 |
| headline | h1 | "[TEXTO DO BRANDING]" | text-2xl md:text-3xl font-bold text-center text-[#COR_PRIMARIA] |
| subheadline | p | "[TEXTO DO BRANDING]" | text-base text-gray-600 text-center mt-2 |
| prova_social | p | "[TEXTO DO BRANDING]" | text-sm text-gray-500 text-center mt-4 |
| btn_comecar | button | "[TEXTO DO CTA]" | w-full py-4 bg-[#COR_PRIMARIA] text-white rounded-lg font-semibold text-lg hover:bg-[#COR_HOVER] transition-all |
| texto_confianca | p | "[TEXTO]" | text-xs text-gray-400 text-center mt-3 |

#### Dados

- **Input:** Nenhum (tela inicial)
- **Output:** `session_id` (gerado automaticamente)

#### Comportamentos

- Ao carregar: Gerar `session_id` único
- Ao clicar em `btn_comecar`: Ir para Tela 2
- Webhook: Disparar `evt_inicio_sessao`

#### Navegação

- **Vem de:** URL direta (entrada do app)
- **Vai para:** Tela 2 (primeira pergunta)

🚦 AVANÇA QUANDO: Tela de entrada especificada
```

---

## 7.3.3 — GERAR ESPECIFICAÇÃO: TELAS DE PERGUNTA

```
[AUTOMÁTICO] Detalhar cada tela de pergunta

Para cada pergunta definida no Blueprint, gerar:

### TELA [N]: PERGUNTA [X] — [NOME]

**ID:** `tela_pergunta_[x]_001`
**Tipo:** PERGUNTA
**Arquivo:** `/src/screens/TelaPergunta[X].jsx`

#### Layout
```
┌─────────────────────────────────────┐
│    [Indicador de progresso]        │
│    ████████░░░░░░░░ 3 de 5         │
│                                     │
│    [PERGUNTA]                       │
│    "[Texto da pergunta]"           │
│                                     │
│    [OPÇÕES DE RESPOSTA]             │
│    ┌─────────────────────────┐     │
│    │   Opção A               │     │
│    └─────────────────────────┘     │
│    ┌─────────────────────────┐     │
│    │   Opção B               │     │
│    └─────────────────────────┘     │
│    ┌─────────────────────────┐     │
│    │   Opção C               │     │
│    └─────────────────────────┘     │
│                                     │
│    [ou INPUT se for campo aberto]  │
│    ┌─────────────────────────┐     │
│    │   [placeholder]         │     │
│    └─────────────────────────┘     │
│    [BOTÃO CONTINUAR]               │
└─────────────────────────────────────┘
```

#### Elementos — TIPO BOTÕES

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| progresso | div | "Pergunta X de Y" | w-full h-2 bg-gray-200 rounded-full |
| progresso_fill | div | - | h-full bg-[#COR_PRIMARIA] rounded-full transition-all |
| pergunta | h2 | "[TEXTO EXATO]" | text-xl font-semibold text-center text-gray-800 |
| opcao_[n] | button | "[TEXTO OPÇÃO]" | w-full py-3 border-2 border-gray-200 rounded-lg text-left px-4 hover:border-[#COR_PRIMARIA] transition-all |
| opcao_selecionada | button | - | border-[#COR_PRIMARIA] bg-[#COR_PRIMARIA]/10 |

#### Elementos — TIPO INPUT

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| input_[nome] | input | placeholder: "[TEXTO]" | w-full py-3 px-4 border-2 border-gray-200 rounded-lg focus:border-[#COR_PRIMARIA] outline-none |
| btn_continuar | button | "Continuar" | w-full py-4 bg-[#COR_PRIMARIA] text-white rounded-lg font-semibold disabled:opacity-50 disabled:cursor-not-allowed |

#### Dados

- **Input:** Respostas anteriores (se necessário para lógica)
- **Output:** `[nome_variavel]` = valor selecionado/digitado
- **Tipo:** [string | number | array]
- **Validação:** [obrigatório | min X | max Y | regex]

#### Comportamentos

- SE tipo botões:
  - Ao clicar em opção: Selecionar e ir para próxima tela automaticamente
  - Ou: Selecionar, habilitar "Continuar", ir ao clicar
- SE tipo input:
  - Ao digitar: Habilitar "Continuar" quando válido
  - Ao clicar "Continuar": Salvar e ir para próxima tela
- Validação:
  - Se inválido: Mostrar borda vermelha + mensagem "[TEXTO ERRO]"

#### Navegação

- **Vem de:** Tela [N-1]
- **Vai para:** Tela [N+1]

[REPETIR PARA CADA PERGUNTA]

🚦 AVANÇA QUANDO: Todas as perguntas especificadas
```

---

## 7.3.4 — GERAR ESPECIFICAÇÃO: TELA DE CAPTURA

```
[AUTOMÁTICO] Detalhar tela de captura de lead (se houver)

### TELA [N]: CAPTURA DE LEAD

**ID:** `tela_captura_001`
**Tipo:** CAPTURA
**Arquivo:** `/src/screens/TelaCaptura.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│                                     │
│    [HEADLINE DE CAPTURA]            │
│    "[Para ver seu resultado...]"   │
│                                     │
│    [Justificativa]                  │
│    "[Por que pedimos isso]"        │
│                                     │
│    [CAMPOS]                         │
│    ┌─────────────────────────┐     │
│    │ Nome                     │     │
│    └─────────────────────────┘     │
│    ┌─────────────────────────┐     │
│    │ WhatsApp                 │     │
│    └─────────────────────────┘     │
│    ┌─────────────────────────┐     │
│    │ Email (opcional)        │     │
│    └─────────────────────────┘     │
│                                     │
│    [BOTÃO ENVIAR]                   │
│                                     │
│    [Texto de privacidade]          │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| headline | h2 | "[TEXTO]" | text-xl font-semibold text-center |
| justificativa | p | "[TEXTO]" | text-sm text-gray-500 text-center mt-2 |
| input_nome | input | placeholder: "Seu nome" | [estilos padrão] |
| input_whatsapp | input | placeholder: "(11) 99999-9999", type: "tel" | [estilos padrão] |
| input_email | input | placeholder: "seu@email.com", type: "email" | [estilos padrão] |
| btn_enviar | button | "[TEXTO CTA]" | [estilos padrão de CTA] |
| privacidade | p | "Seus dados estão seguros..." | text-xs text-gray-400 text-center mt-4 |

#### Dados

- **Input:** Dados das perguntas anteriores
- **Output:** 
  - `lead_nome`: string, obrigatório
  - `lead_whatsapp`: string, obrigatório, formato: (XX) XXXXX-XXXX
  - `lead_email`: string, opcional

#### Validações

```javascript
// Nome: mínimo 2 caracteres
if (nome.length < 2) erro = "Digite seu nome completo"

// WhatsApp: formato brasileiro
const regexWhats = /^\(\d{2}\)\s?\d{4,5}-?\d{4}$/
if (!regexWhats.test(whatsapp)) erro = "WhatsApp inválido"

// Email: formato válido (se preenchido)
const regexEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
if (email && !regexEmail.test(email)) erro = "Email inválido"
```

#### Comportamentos

- Ao carregar: Focar no primeiro campo
- Ao digitar: Validar em tempo real
- Ao clicar "Enviar":
  1. Validar todos os campos
  2. Se válido: Salvar dados + disparar webhook + ir para Loading
  3. Se inválido: Mostrar erros
- Webhook: `evt_lead_capturado` com todos os dados

#### Navegação

- **Vem de:** Última pergunta
- **Vai para:** Tela de Loading

🚦 AVANÇA QUANDO: Tela de captura especificada
```

---

## 7.3.5 — GERAR ESPECIFICAÇÃO: TELA DE LOADING

```
[AUTOMÁTICO] Detalhar tela de loading/suspense

### TELA [N]: LOADING

**ID:** `tela_loading_001`
**Tipo:** LOADING
**Arquivo:** `/src/screens/TelaLoading.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│         [ANIMAÇÃO DE LOADING]       │
│              ◐ ◓ ◑ ◒                │
│                                     │
│    [TEXTO ROTATIVO]                 │
│    "Analisando suas respostas..."  │
│                                     │
│    [Barra de progresso animada]    │
│    ████████████░░░░░░░░░░░░        │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| animacao | div | Spinner ou ícone animado | w-16 h-16 mx-auto animate-spin |
| texto_rotativo | p | [Array de textos] | text-lg text-gray-600 text-center transition-all |
| barra_progresso | div | - | w-full h-2 bg-gray-200 rounded-full overflow-hidden |
| barra_fill | div | - | h-full bg-[#COR_PRIMARIA] animate-progress |

#### Textos Rotativos (trocar a cada 1.5s)

```javascript
const textos = [
  "[TEXTO 1 DO BRANDING]",
  "[TEXTO 2 DO BRANDING]",
  "[TEXTO 3 DO BRANDING]",
  "[TEXTO 4 DO BRANDING]"
]
```

#### Comportamentos

- Ao carregar:
  1. Iniciar animação de loading
  2. Iniciar rotação de textos (a cada 1.5s)
  3. Executar cálculo do resultado em background
  4. Após [X] segundos OU cálculo completo: Ir para Resultado
- Duração mínima: 3 segundos (para criar suspense)
- Duração máxima: 8 segundos (fallback se cálculo demorar)

#### Dados

- **Input:** Todas as respostas coletadas
- **Output:** Resultado calculado (salvo em estado)
- **Processamento:** Executar `calcularResultado(dados)`

#### Navegação

- **Vem de:** Tela de Captura (ou última pergunta)
- **Vai para:** Tela de Resultado (automático)

🚦 AVANÇA QUANDO: Tela de loading especificada
```

---

## 7.3.6 — GERAR ESPECIFICAÇÃO: TELA DE RESULTADO

```
[AUTOMÁTICO] Detalhar tela de resultado (momento caneta)

### TELA [N]: RESULTADO

**ID:** `tela_resultado_001`
**Tipo:** RESULTADO
**Arquivo:** `/src/screens/TelaResultado.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│                                     │
│    [HEADLINE DE REVELAÇÃO]          │
│    "{nome}, seu resultado:"        │
│                                     │
│    ┌─────────────────────────┐     │
│    │                         │     │
│    │    [RESULTADO DESTAQUE] │     │
│    │       R$ 12.450         │     │
│    │    [contexto]           │     │
│    │                         │     │
│    └─────────────────────────┘     │
│                                     │
│    [Explicação/breakdown]          │
│    "Isso significa que..."         │
│                                     │
│    [BOTÃO CONTINUAR]               │
│                                     │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| headline | h1 | "[TEXTO com {nome}]" | text-xl font-semibold text-center |
| card_resultado | div | - | bg-gradient-to-br from-[#COR1] to-[#COR2] rounded-2xl p-8 text-center shadow-lg |
| resultado_valor | p | "{resultado_formatado}" | text-4xl md:text-5xl font-bold text-white |
| resultado_contexto | p | "[TEXTO]" | text-lg text-white/80 mt-2 |
| explicacao | p | "[TEXTO com {variaveis}]" | text-base text-gray-600 text-center mt-6 |
| btn_continuar | button | "[TEXTO]" | [estilos padrão de CTA] |

#### Dados

- **Input:** Resultado calculado (do estado)
- **Output:** Exibição formatada
- **Variáveis disponíveis:**
  - `{nome}` — nome do lead
  - `{resultado}` — valor principal formatado
  - `{[outras variaveis]}` — conforme cálculo

#### Formatação do Resultado

```javascript
// Exemplo para valor monetário
const formatarResultado = (valor) => {
  return valor.toLocaleString('pt-BR', {
    style: 'currency',
    currency: 'BRL'
  })
}

// Exemplo para porcentagem
const formatarPorcentagem = (valor) => {
  return `${valor.toFixed(1)}%`
}
```

#### Comportamentos

- Ao carregar:
  1. Buscar resultado do estado
  2. Aplicar formatação
  3. Substituir variáveis nos textos
  4. Disparar webhook `evt_resultado_gerado`
- Animação de entrada: Fade-in + scale do card (300ms)
- Ao clicar "Continuar": Ir para próxima tela (persuasão ou CTA)

#### Navegação

- **Vem de:** Tela de Loading
- **Vai para:** Tela de Persuasão ou CTA (conforme fluxo)

🚦 AVANÇA QUANDO: Tela de resultado especificada
```

---

## 7.3.7 — GERAR ESPECIFICAÇÃO: TELAS DE PERSUASÃO

```
[AUTOMÁTICO] Detalhar telas de persuasão (se houver)

PARA CADA TELA DE PERSUASÃO DEFINIDA NO BLUEPRINT:

### TELA [N]: [TIPO DE PERSUASÃO]

**Tipos possíveis:**
- PROVA_SOCIAL (depoimentos)
- ANTES_DEPOIS (transformação visual)
- AUTORIDADE (credenciais)
- GARANTIA (segurança)
- OBJECOES (FAQ)
- FUTURO_DESEJADO (visualização)

[Especificar cada uma conforme template padrão]

🚦 AVANÇA QUANDO: Telas de persuasão especificadas
```

---

## 7.3.8 — GERAR ESPECIFICAÇÃO: TELA DE CTA FINAL

```
[AUTOMÁTICO] Detalhar tela de CTA

### TELA [N]: CTA FINAL

**ID:** `tela_cta_001`
**Tipo:** CTA
**Arquivo:** `/src/screens/TelaCTA.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│                                     │
│    [HEADLINE DO CTA]                │
│    "[Texto de chamada final]"      │
│                                     │
│    [Reforço de benefício]          │
│    "[O que ganha ao clicar]"       │
│                                     │
│    ┌─────────────────────────┐     │
│    │   [BOTÃO CTA PRINCIPAL]  │     │
│    │   🟢 Falar com Especialista   │
│    └─────────────────────────┘     │
│                                     │
│    [CTA secundário] (opcional)     │
│    "ou agende uma ligação"         │
│                                     │
│    [Texto de urgência]             │
│    "[Escassez/urgência se houver]" │
│                                     │
└─────────────────────────────────────┘
```

#### Elementos

| Elemento | Tipo | Conteúdo | Estilo |
|----------|------|----------|--------|
| headline | h2 | "[TEXTO]" | text-xl font-bold text-center |
| reforco | p | "[TEXTO]" | text-base text-gray-600 text-center mt-2 |
| btn_cta | button/a | "[TEXTO + ÍCONE]" | w-full py-4 bg-[#25D366] text-white rounded-lg font-bold text-lg flex items-center justify-center gap-2 hover:bg-[#128C7E] transition-all |
| cta_secundario | a | "[TEXTO]" | text-sm text-[#COR_PRIMARIA] underline text-center block mt-4 |
| urgencia | p | "[TEXTO]" | text-sm text-red-500 text-center mt-4 |

#### Ação do CTA — WHATSAPP

```javascript
const abrirWhatsApp = () => {
  // Montar mensagem com variáveis
  const mensagem = `[TEMPLATE_MENSAGEM]`
    .replace('{nome}', lead_nome)
    .replace('{resultado}', resultado_formatado)
    // ... outras variáveis
  
  // Codificar para URL
  const mensagemCodificada = encodeURIComponent(mensagem)
  
  // Montar URL
  const url = `https://wa.me/${WHATSAPP_NUMERO}?text=${mensagemCodificada}`
  
  // Abrir
  window.open(url, '_blank')
  
  // Disparar webhook
  dispararWebhook('evt_cta_clicado', { tipo: 'whatsapp', lead_id })
}
```

#### Template da Mensagem WhatsApp

```
[MENSAGEM EXATA DEFINIDA NA OFERTA]

Exemplo:
"Olá! Acabei de fazer o diagnóstico no site e descobri que posso 
economizar {resultado} por ano. Meu nome é {nome} e gostaria de 
saber mais sobre como vocês podem me ajudar."
```

#### Ação do CTA — PÁGINA EXTERNA

```javascript
const abrirPagina = () => {
  window.open('[URL_DESTINO]', '_blank')
  dispararWebhook('evt_cta_clicado', { tipo: 'pagina', lead_id })
}
```

#### Ação do CTA — AGENDAMENTO

```javascript
const abrirAgendamento = () => {
  // Se usar Calendly, por exemplo
  window.open('[URL_CALENDLY]', '_blank')
  dispararWebhook('evt_cta_clicado', { tipo: 'agendamento', lead_id })
}
```

#### Comportamentos

- Ao carregar: Disparar `evt_tela_cta_vista`
- Ao clicar CTA principal: Executar ação + webhook
- Ao clicar CTA secundário: Executar ação alternativa

#### Navegação

- **Vem de:** Tela de Resultado ou última tela de persuasão
- **Vai para:** Ação externa (WhatsApp, página, etc.)

🚦 AVANÇA QUANDO: Tela de CTA especificada
```

---

## 7.3.9 — GERAR ESPECIFICAÇÃO: TELA DE CONFIRMAÇÃO (SE HOUVER)

```
[AUTOMÁTICO] Detalhar tela de confirmação (se houver)

### TELA [N]: CONFIRMAÇÃO

**ID:** `tela_confirmacao_001`
**Tipo:** CONFIRMACAO
**Arquivo:** `/src/screens/TelaConfirmacao.jsx`

#### Layout
```
┌─────────────────────────────────────┐
│                                     │
│         ✅                          │
│                                     │
│    [HEADLINE DE SUCESSO]            │
│    "Perfeito, {nome}!"             │
│                                     │
│    [Mensagem de próximos passos]   │
│    "Em instantes você receberá..." │
│                                     │
│    [Expectativas]                   │
│    "• Responderemos em até 2h"     │
│    "• Prepare seus documentos"     │
│                                     │
└─────────────────────────────────────┘
```

#### Elementos e comportamentos

[Especificar conforme template padrão]

🚦 AVANÇA QUANDO: Tela de confirmação especificada (se aplicável)
```

---

## 7.3.10 — CONSOLIDAR SEÇÃO DE TELAS

```
[REGISTRO] Juntar todas as especificações de tela

SEÇÃO 2 DO PROMPT MESTRE:
├── Template de especificação
├── Tela de Entrada
├── Telas de Perguntas (×N)
├── Tela de Captura (se houver)
├── Tela de Loading
├── Tela de Resultado
├── Telas de Persuasão (×N, se houver)
├── Tela de CTA
└── Tela de Confirmação (se houver)

TOTAL: [X] telas especificadas

🚦 AVANÇA QUANDO: Seção 2 consolidada
```

---

# BLOCO 7.4: MONTAR SEÇÃO DE LÓGICA DE NEGÓCIO

**Objetivo:** Especificar toda a lógica de cálculo em código executável.

---

## 7.4.1 — GERAR FUNÇÃO PRINCIPAL DE CÁLCULO

```
[AUTOMÁTICO] Criar código completo da lógica

## LÓGICA DE NEGÓCIO

### Função Principal: calcularResultado

```javascript
/**
 * Calcula o resultado principal da isca
 * @param {Object} dados - Dados coletados nas perguntas
 * @returns {Object} Resultado calculado
 */
function calcularResultado(dados) {
  // Extrair variáveis
  const {
    [nome_var_1],
    [nome_var_2],
    [nome_var_3],
    // ... todas as variáveis
  } = dados

  // [LÓGICA ESPECÍFICA DO TIPO DE ISCA]
  
  // EXEMPLO PARA CALCULADORA:
  // Converter valores se necessário
  const valorNumerico = parseFloat(nome_var_1.replace(/[^\d,]/g, '').replace(',', '.'))
  
  // Aplicar fórmula
  const resultado = valorNumerico * [FATOR] * [OUTRO_FATOR]
  
  // OU EXEMPLO PARA DIAGNÓSTICO:
  // Calcular pontuação
  let pontuacao = 0
  if (nome_var_1 === 'opcao_a') pontuacao += 10
  if (nome_var_1 === 'opcao_b') pontuacao += 5
  // ... outras regras
  
  // Determinar faixa
  let faixa = 'baixo'
  if (pontuacao > 50) faixa = 'medio'
  if (pontuacao > 80) faixa = 'alto'

  // Retornar resultado estruturado
  return {
    principal: resultado, // ou pontuacao
    faixa: faixa,        // se aplicável
    detalhes: {
      // breakdown se houver
    }
  }
}
```

🚦 AVANÇA QUANDO: Função principal criada
```

---

## 7.4.2 — GERAR FUNÇÕES AUXILIARES

```
[AUTOMÁTICO] Criar funções de apoio

### Funções Auxiliares

```javascript
/**
 * Formata valor como moeda brasileira
 */
function formatarMoeda(valor) {
  return valor.toLocaleString('pt-BR', {
    style: 'currency',
    currency: 'BRL',
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
  })
}

/**
 * Formata número com separador de milhares
 */
function formatarNumero(valor) {
  return valor.toLocaleString('pt-BR')
}

/**
 * Formata porcentagem
 */
function formatarPorcentagem(valor) {
  return `${valor.toFixed(1)}%`
}

/**
 * Formata telefone brasileiro
 */
function formatarTelefone(valor) {
  const limpo = valor.replace(/\D/g, '')
  if (limpo.length === 11) {
    return `(${limpo.slice(0,2)}) ${limpo.slice(2,7)}-${limpo.slice(7)}`
  }
  return valor
}

/**
 * Valida email
 */
function validarEmail(email) {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return regex.test(email)
}

/**
 * Valida WhatsApp brasileiro
 */
function validarWhatsApp(numero) {
  const limpo = numero.replace(/\D/g, '')
  return limpo.length === 10 || limpo.length === 11
}

/**
 * Gera ID único para sessão
 */
function gerarSessionId() {
  return `sess_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
}
```

🚦 AVANÇA QUANDO: Funções auxiliares criadas
```

---

## 7.4.3 — GERAR CASOS DE TESTE

```
[AUTOMÁTICO] Criar exemplos de input → output

### Casos de Teste

Use estes casos para validar se a lógica está funcionando:

#### Caso 1: [Descrição]
```javascript
// Input
const dados1 = {
  [var1]: "[valor1]",
  [var2]: "[valor2]",
  // ...
}

// Output esperado
const esperado1 = {
  principal: [valor],
  faixa: "[faixa]",
  // ...
}

// Teste
console.assert(
  calcularResultado(dados1).principal === esperado1.principal,
  'Caso 1 falhou'
)
```

#### Caso 2: [Descrição]
```javascript
// Input
const dados2 = { /* ... */ }

// Output esperado
const esperado2 = { /* ... */ }
```

#### Caso 3: Valores extremos
```javascript
// Input mínimo
const dadosMin = { /* valores mínimos possíveis */ }

// Input máximo
const dadosMax = { /* valores máximos possíveis */ }
```

🚦 AVANÇA QUANDO: Casos de teste criados
```

---

## 7.4.4 — GERAR GERENCIAMENTO DE ESTADO

```
[AUTOMÁTICO] Definir como dados fluem no app

### Gerenciamento de Estado

#### Estado Global do App

```javascript
// Estado inicial
const estadoInicial = {
  // Controle de fluxo
  telaAtual: 0,
  sessionId: null,
  
  // Respostas das perguntas
  respostas: {
    [pergunta_1]: null,
    [pergunta_2]: null,
    // ... todas as perguntas
  },
  
  // Dados do lead
  lead: {
    nome: null,
    whatsapp: null,
    email: null
  },
  
  // Resultado
  resultado: null,
  resultadoFormatado: null,
  
  // Controle
  carregando: false,
  erro: null
}
```

#### Ações do Estado

```javascript
// Ações disponíveis
const acoes = {
  INICIAR_SESSAO: 'INICIAR_SESSAO',
  RESPONDER_PERGUNTA: 'RESPONDER_PERGUNTA',
  SALVAR_LEAD: 'SALVAR_LEAD',
  CALCULAR_RESULTADO: 'CALCULAR_RESULTADO',
  AVANCAR_TELA: 'AVANCAR_TELA',
  VOLTAR_TELA: 'VOLTAR_TELA',
  SET_ERRO: 'SET_ERRO',
  LIMPAR_ERRO: 'LIMPAR_ERRO'
}
```

#### Reducer (se usar useReducer)

```javascript
function reducer(estado, acao) {
  switch (acao.type) {
    case 'INICIAR_SESSAO':
      return {
        ...estado,
        sessionId: gerarSessionId()
      }
    
    case 'RESPONDER_PERGUNTA':
      return {
        ...estado,
        respostas: {
          ...estado.respostas,
          [acao.pergunta]: acao.valor
        }
      }
    
    case 'SALVAR_LEAD':
      return {
        ...estado,
        lead: acao.dados
      }
    
    case 'CALCULAR_RESULTADO':
      const resultado = calcularResultado(estado.respostas)
      return {
        ...estado,
        resultado: resultado,
        resultadoFormatado: formatarResultado(resultado)
      }
    
    case 'AVANCAR_TELA':
      return {
        ...estado,
        telaAtual: estado.telaAtual + 1
      }
    
    default:
      return estado
  }
}
```

🚦 AVANÇA QUANDO: Gerenciamento de estado definido
```

---

## 7.4.5 — GERAR LÓGICA CONDICIONAL (SE HOUVER)

```
[AUTOMÁTICO] Definir branching se houver caminhos diferentes

### Lógica Condicional (se aplicável)

SE a isca tiver caminhos diferentes baseados em respostas:

```javascript
/**
 * Determina próxima tela baseado em respostas
 */
function determinarProximaTela(telaAtual, respostas) {
  // Exemplo: pular tela 3 se resposta específica
  if (telaAtual === 2 && respostas.pergunta_2 === 'opcao_c') {
    return 4 // pula tela 3
  }
  
  // Exemplo: caminho diferente por perfil
  if (telaAtual === 5) {
    if (respostas.perfil === 'empresa') {
      return 6 // caminho empresa
    } else {
      return 7 // caminho pessoa física
    }
  }
  
  // Padrão: próxima tela sequencial
  return telaAtual + 1
}
```

🚦 AVANÇA QUANDO: Lógica condicional definida (se aplicável)
```

---

## 7.4.6 — GERAR TRATAMENTO DE ERROS

```
[AUTOMÁTICO] Definir como lidar com erros

### Tratamento de Erros

```javascript
/**
 * Wrapper para operações que podem falhar
 */
async function operacaoSegura(operacao, fallback = null) {
  try {
    return await operacao()
  } catch (erro) {
    console.error('Erro na operação:', erro)
    return fallback
  }
}

/**
 * Mensagens de erro amigáveis
 */
const mensagensErro = {
  CAMPO_OBRIGATORIO: 'Este campo é obrigatório',
  FORMATO_INVALIDO: 'Formato inválido',
  WHATSAPP_INVALIDO: 'Digite um WhatsApp válido',
  EMAIL_INVALIDO: 'Digite um email válido',
  ERRO_CALCULO: 'Erro ao calcular resultado. Tente novamente.',
  ERRO_CONEXAO: 'Erro de conexão. Verifique sua internet.',
  ERRO_GENERICO: 'Algo deu errado. Tente novamente.'
}

/**
 * Exibir erro na tela
 */
function mostrarErro(tipo, campo = null) {
  const mensagem = mensagensErro[tipo] || mensagensErro.ERRO_GENERICO
  // Atualizar estado com erro
  dispatch({ type: 'SET_ERRO', erro: { tipo, mensagem, campo } })
  // Auto-limpar após 5s
  setTimeout(() => dispatch({ type: 'LIMPAR_ERRO' }), 5000)
}
```

🚦 AVANÇA QUANDO: Tratamento de erros definido
```

---

## 7.4.7 — GERAR VALIDAÇÕES

```
[AUTOMÁTICO] Criar todas as validações de campos

### Validações de Campos

```javascript
/**
 * Validar campo individual
 */
function validarCampo(campo, valor, regras) {
  // Obrigatório
  if (regras.obrigatorio && (!valor || valor.trim() === '')) {
    return { valido: false, erro: 'Este campo é obrigatório' }
  }
  
  // Mínimo de caracteres
  if (regras.minLength && valor.length < regras.minLength) {
    return { valido: false, erro: `Mínimo ${regras.minLength} caracteres` }
  }
  
  // Máximo de caracteres
  if (regras.maxLength && valor.length > regras.maxLength) {
    return { valido: false, erro: `Máximo ${regras.maxLength} caracteres` }
  }
  
  // Valor mínimo (numérico)
  if (regras.min !== undefined) {
    const num = parseFloat(valor)
    if (isNaN(num) || num < regras.min) {
      return { valido: false, erro: `Valor mínimo: ${regras.min}` }
    }
  }
  
  // Valor máximo (numérico)
  if (regras.max !== undefined) {
    const num = parseFloat(valor)
    if (isNaN(num) || num > regras.max) {
      return { valido: false, erro: `Valor máximo: ${regras.max}` }
    }
  }
  
  // Regex personalizado
  if (regras.pattern && !regras.pattern.test(valor)) {
    return { valido: false, erro: regras.mensagemPattern || 'Formato inválido' }
  }
  
  // Validações específicas
  if (regras.tipo === 'email' && !validarEmail(valor)) {
    return { valido: false, erro: 'Email inválido' }
  }
  
  if (regras.tipo === 'whatsapp' && !validarWhatsApp(valor)) {
    return { valido: false, erro: 'WhatsApp inválido' }
  }
  
  return { valido: true, erro: null }
}

/**
 * Regras de validação por campo
 */
const regrasValidacao = {
  [campo_1]: { obrigatorio: true, tipo: '[tipo]' },
  [campo_2]: { obrigatorio: true, min: 0, max: 1000000 },
  lead_nome: { obrigatorio: true, minLength: 2 },
  lead_whatsapp: { obrigatorio: true, tipo: 'whatsapp' },
  lead_email: { obrigatorio: false, tipo: 'email' }
}
```

🚦 AVANÇA QUANDO: Validações criadas
```

---

## 7.4.8 — CONSOLIDAR SEÇÃO DE LÓGICA

```
[REGISTRO] Juntar toda a lógica de negócio

SEÇÃO 3 DO PROMPT MESTRE:
├── Função principal de cálculo
├── Funções auxiliares (formatação, validação)
├── Casos de teste
├── Gerenciamento de estado
├── Lógica condicional (se houver)
├── Tratamento de erros
└── Validações de campos

🚦 AVANÇA QUANDO: Seção 3 consolidada
```

---

# BLOCO 7.5: MONTAR SEÇÃO DE INTEGRAÇÕES E APIs

**Objetivo:** Especificar todas as integrações externas.

📦 **MATERIAIS:** 
- Decisões de integração da Fase 6
- `APIS_POR_FUNCAO.md`
- Webhooks definidos

---

## 7.5.1 — GERAR INTEGRAÇÃO COM ARMAZENAMENTO

```
[AUTOMÁTICO] Criar código de armazenamento

## INTEGRAÇÕES

### Armazenamento de Dados

#### SE USAR SUPABASE:

```javascript
import { createClient } from '@supabase/supabase-js'

// Inicializar cliente
const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_ANON_KEY
)

/**
 * Salvar lead no banco
 */
async function salvarLead(dados) {
  const { data, error } = await supabase
    .from('leads')
    .insert([
      {
        session_id: dados.sessionId,
        nome: dados.nome,
        whatsapp: dados.whatsapp,
        email: dados.email,
        respostas: JSON.stringify(dados.respostas),
        resultado: JSON.stringify(dados.resultado),
        created_at: new Date().toISOString(),
        utm_source: dados.utm_source || null,
        utm_medium: dados.utm_medium || null,
        utm_campaign: dados.utm_campaign || null
      }
    ])
    .select()
  
  if (error) {
    console.error('Erro ao salvar lead:', error)
    throw error
  }
  
  return data[0]
}

/**
 * Estrutura da tabela 'leads'
 */
// SQL para criar a tabela:
/*
CREATE TABLE leads (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  session_id TEXT NOT NULL,
  nome TEXT NOT NULL,
  whatsapp TEXT NOT NULL,
  email TEXT,
  respostas JSONB,
  resultado JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  utm_source TEXT,
  utm_medium TEXT,
  utm_campaign TEXT
);
*/
```

#### SE USAR GOOGLE SHEETS:

```javascript
/**
 * Enviar dados para Google Sheets via webhook
 */
async function salvarNoSheets(dados) {
  const webhookUrl = process.env.GOOGLE_SHEETS_WEBHOOK_URL
  
  const response = await fetch(webhookUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      nome: dados.nome,
      whatsapp: dados.whatsapp,
      email: dados.email,
      resultado: dados.resultado,
      data: new Date().toLocaleString('pt-BR'),
      // ... outros campos
    })
  })
  
  if (!response.ok) {
    throw new Error('Erro ao salvar no Sheets')
  }
}
```

🚦 AVANÇA QUANDO: Armazenamento especificado
```

---

## 7.5.2 — GERAR INTEGRAÇÃO COM WEBHOOKS

```
[AUTOMÁTICO] Criar código dos webhooks

### Webhooks

```javascript
/**
 * Disparar webhook genérico
 */
async function dispararWebhook(evento, dados) {
  // Mapear evento para URL
  const webhookUrls = {
    'evt_inicio_sessao': process.env.WEBHOOK_INICIO_URL,
    'evt_lead_capturado': process.env.WEBHOOK_LEAD_URL,
    'evt_resultado_gerado': process.env.WEBHOOK_RESULTADO_URL,
    'evt_cta_clicado': process.env.WEBHOOK_CTA_URL,
    'evt_abandono': process.env.WEBHOOK_ABANDONO_URL
  }
  
  const url = webhookUrls[evento]
  
  // Se URL não configurada, ignorar silenciosamente
  if (!url) {
    console.log(`Webhook ${evento} não configurado, ignorando.`)
    return
  }
  
  try {
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        evento: evento,
        timestamp: new Date().toISOString(),
        dados: dados
      })
    })
    
    if (!response.ok) {
      console.error(`Webhook ${evento} falhou:`, response.status)
    }
  } catch (error) {
    // Não bloquear o app se webhook falhar
    console.error(`Erro no webhook ${evento}:`, error)
  }
}
```

### Payloads dos Webhooks

#### evt_inicio_sessao
```json
{
  "evento": "evt_inicio_sessao",
  "timestamp": "2024-01-15T10:30:00.000Z",
  "dados": {
    "session_id": "sess_1705312200_abc123",
    "utm_source": "instagram",
    "utm_medium": "cpc",
    "utm_campaign": "isca_janeiro"
  }
}
```

#### evt_lead_capturado
```json
{
  "evento": "evt_lead_capturado",
  "timestamp": "2024-01-15T10:32:00.000Z",
  "dados": {
    "session_id": "sess_1705312200_abc123",
    "lead": {
      "nome": "João Silva",
      "whatsapp": "11999999999",
      "email": "joao@email.com"
    },
    "respostas": {
      "pergunta_1": "opcao_a",
      "pergunta_2": "50000"
    }
  }
}
```

#### evt_resultado_gerado
```json
{
  "evento": "evt_resultado_gerado",
  "timestamp": "2024-01-15T10:32:05.000Z",
  "dados": {
    "session_id": "sess_1705312200_abc123",
    "resultado": {
      "principal": 12450,
      "formatado": "R$ 12.450,00",
      "faixa": "alto"
    }
  }
}
```

#### evt_cta_clicado
```json
{
  "evento": "evt_cta_clicado",
  "timestamp": "2024-01-15T10:33:00.000Z",
  "dados": {
    "session_id": "sess_1705312200_abc123",
    "tipo_cta": "whatsapp",
    "destino": "5511999999999"
  }
}
```

🚦 AVANÇA QUANDO: Webhooks especificados
```

---

## 7.5.3 — GERAR INTEGRAÇÃO COM WHATSAPP

```
[AUTOMÁTICO] Criar código do CTA WhatsApp

### Integração WhatsApp

```javascript
/**
 * Abrir WhatsApp com mensagem pré-preenchida
 */
function abrirWhatsApp(dados) {
  // Número de destino (sem formatação)
  const numero = process.env.WHATSAPP_NUMERO.replace(/\D/g, '')
  
  // Template da mensagem
  const template = `[MENSAGEM_TEMPLATE_DA_OFERTA]`
  
  // Substituir variáveis
  const mensagem = template
    .replace('{nome}', dados.lead.nome)
    .replace('{resultado}', dados.resultadoFormatado)
    .replace('{[outra_var]}', dados.[outra_var])
    // ... outras substituições
  
  // Codificar para URL
  const mensagemCodificada = encodeURIComponent(mensagem)
  
  // Montar URL final
  const url = `https://wa.me/${numero}?text=${mensagemCodificada}`
  
  // Abrir em nova aba
  window.open(url, '_blank')
  
  // Disparar webhook
  dispararWebhook('evt_cta_clicado', {
    session_id: dados.sessionId,
    tipo_cta: 'whatsapp',
    destino: numero
  })
}
```

### Template da Mensagem

```
[TEXTO EXATO DEFINIDO NA OFERTA]

Exemplo:
---
Olá! 👋

Acabei de fazer o [NOME_DA_ISCA] no site e descobri que posso {resultado}.

Meu nome é {nome} e gostaria de saber mais sobre como vocês podem me ajudar!
---
```

🚦 AVANÇA QUANDO: WhatsApp especificado
```

---

## 7.5.4 — GERAR INTEGRAÇÃO COM API DE IMAGEM (SE MODO 1)

```
[CONDICIONAL] Só executar se modo_imagem = 1

### Integração com API de Imagem

#### API Escolhida: [NOME_DA_API]

```javascript
/**
 * Gerar imagem via API
 */
async function gerarImagem(dados) {
  const apiKey = process.env.[NOME_API]_API_KEY
  const endpoint = '[ENDPOINT_DA_API]'
  
  try {
    const response = await fetch(endpoint, {
      method: '[METODO]',
      headers: {
        'Authorization': `Bearer ${apiKey}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        // Payload específico da API
        [campo_1]: dados.[campo_1],
        [campo_2]: dados.[campo_2],
        // ...
      })
    })
    
    if (!response.ok) {
      throw new Error(`API retornou ${response.status}`)
    }
    
    const resultado = await response.json()
    
    // Extrair URL da imagem (depende da API)
    return resultado.[campo_imagem_url]
    
  } catch (error) {
    console.error('Erro ao gerar imagem:', error)
    // Retornar fallback
    return '/assets/imagem_fallback.png'
  }
}
```

#### Variável de Ambiente

```env
[NOME_API]_API_KEY=sua_chave_aqui
```

#### Fallback

Se a API falhar, usar imagem estática de `/assets/imagem_fallback.png`.

🚦 AVANÇA QUANDO: API de imagem especificada (se aplicável)
```

---

## 7.5.5 — GERAR LÓGICA DE CURADORIA (SE MODO 2)

```
[CONDICIONAL] Só executar se modo_imagem = 2

### Curadoria de Imagens

```javascript
/**
 * Selecionar imagem do banco baseado no resultado
 */
function selecionarImagem(resultado, respostas) {
  // Banco de imagens por categoria
  const bancoImagens = {
    'resultado_baixo': '/assets/imagens/resultado_baixo.png',
    'resultado_medio': '/assets/imagens/resultado_medio.png',
    'resultado_alto': '/assets/imagens/resultado_alto.png',
    // ... outras categorias
  }
  
  // Lógica de seleção
  let categoria = 'resultado_medio' // default
  
  if (resultado.faixa === 'alto') {
    categoria = 'resultado_alto'
  } else if (resultado.faixa === 'baixo') {
    categoria = 'resultado_baixo'
  }
  
  // Variação adicional baseada em resposta específica
  if (respostas.[pergunta_x] === '[valor_especifico]') {
    categoria += '_variante_a'
  }
  
  return bancoImagens[categoria] || bancoImagens['resultado_medio']
}
```

#### Imagens Necessárias

O cliente deve fornecer:
- `/assets/imagens/resultado_baixo.png` (ou .jpg)
- `/assets/imagens/resultado_medio.png`
- `/assets/imagens/resultado_alto.png`
- [outras imagens conforme categorias]

🚦 AVANÇA QUANDO: Curadoria especificada (se aplicável)
```

---

## 7.5.6 — GERAR CAPTURA DE UTM

```
[AUTOMÁTICO] Criar código para capturar parâmetros UTM

### Captura de UTM

```javascript
/**
 * Capturar parâmetros UTM da URL
 */
function capturarUTM() {
  const params = new URLSearchParams(window.location.search)
  
  return {
    utm_source: params.get('utm_source') || null,
    utm_medium: params.get('utm_medium') || null,
    utm_campaign: params.get('utm_campaign') || null,
    utm_term: params.get('utm_term') || null,
    utm_content: params.get('utm_content') || null
  }
}

// Usar ao iniciar sessão
useEffect(() => {
  const utms = capturarUTM()
  dispatch({ type: 'SET_UTM', utms })
}, [])
```

🚦 AVANÇA QUANDO: Captura de UTM especificada
```

---

## 7.5.7 — GERAR INTEGRAÇÃO COM CALENDLY (SE APLICÁVEL)

```
[CONDICIONAL] Só executar se CTA for agendamento

### Integração com Calendly

```javascript
/**
 * Abrir Calendly com pré-preenchimento
 */
function abrirCalendly(dados) {
  const calendlyUrl = process.env.CALENDLY_URL
  
  // Parâmetros de pré-preenchimento
  const params = new URLSearchParams({
    name: dados.lead.nome,
    email: dados.lead.email,
    a1: dados.resultadoFormatado // campo personalizado
  })
  
  const urlFinal = `${calendlyUrl}?${params.toString()}`
  
  window.open(urlFinal, '_blank')
  
  dispararWebhook('evt_cta_clicado', {
    session_id: dados.sessionId,
    tipo_cta: 'agendamento',
    destino: calendlyUrl
  })
}
```

🚦 AVANÇA QUANDO: Calendly especificado (se aplicável)
```

---

## 7.5.8 — GERAR TRATAMENTO DE ERROS DE API

```
[AUTOMÁTICO] Criar tratamento robusto

### Tratamento de Erros de Integração

```javascript
/**
 * Wrapper para chamadas de API com retry
 */
async function chamarAPIComRetry(funcao, tentativas = 3, delayMs = 1000) {
  for (let i = 0; i < tentativas; i++) {
    try {
      return await funcao()
    } catch (error) {
      console.error(`Tentativa ${i + 1} falhou:`, error)
      
      if (i === tentativas - 1) {
        // Última tentativa, lançar erro
        throw error
      }
      
      // Esperar antes de tentar novamente
      await new Promise(resolve => setTimeout(resolve, delayMs))
    }
  }
}

/**
 * Usar assim:
 */
const resultado = await chamarAPIComRetry(
  () => gerarImagem(dados),
  3,
  1000
)
```

🚦 AVANÇA QUANDO: Tratamento de erros especificado
```

---

## 7.5.9 — GERAR DOCUMENTAÇÃO DE APIS USADAS

```
[AUTOMÁTICO] Criar referência das APIs

### Referência das APIs Utilizadas

| API | Função | Endpoint | Auth | Variável |
|-----|--------|----------|------|----------|
| [Nome] | [Para que serve] | [URL] | [Tipo] | [ENV_VAR] |
| Supabase | Armazenamento | Dinâmico | anon_key | SUPABASE_URL, SUPABASE_ANON_KEY |
| WhatsApp | CTA | wa.me | N/A | WHATSAPP_NUMERO |
| [API Imagem] | Gerar imagem | [URL] | Bearer | [NOME]_API_KEY |

🚦 AVANÇA QUANDO: Documentação de APIs criada
```

---

## 7.5.10 — CONSOLIDAR SEÇÃO DE INTEGRAÇÕES

```
[REGISTRO] Juntar todas as integrações

SEÇÃO 4 DO PROMPT MESTRE:
├── Armazenamento (Supabase/Sheets)
├── Webhooks (5 eventos)
├── WhatsApp
├── API de Imagem (se modo 1)
├── Curadoria de Imagens (se modo 2)
├── Captura de UTM
├── Calendly (se aplicável)
├── Tratamento de erros
└── Documentação de referência

🚦 AVANÇA QUANDO: Seção 4 consolidada
```

---

# BLOCO 7.6: MONTAR SEÇÃO DE ESTILO E CONFIGURAÇÕES

**Objetivo:** Especificar design visual e configurações finais.

---

## 7.6.1 — GERAR ESPECIFICAÇÃO DE CORES

```
[AUTOMÁTICO] Definir paleta de cores

## ESTILO VISUAL

### Paleta de Cores

```javascript
const cores = {
  // Cores principais
  primaria: '#[HEX_DO_BRANDING]',
  primariaHover: '#[HEX_HOVER]',
  primariaLight: '#[HEX_LIGHT]',
  
  // Cores secundárias
  secundaria: '#[HEX]',
  
  // Cores de estado
  sucesso: '#22C55E',
  alerta: '#F59E0B',
  erro: '#EF4444',
  
  // Cores neutras
  textoEscuro: '#1F2937',
  textoMedio: '#6B7280',
  textoClaro: '#9CA3AF',
  fundoPrincipal: '#FFFFFF',
  fundoSecundario: '#F9FAFB',
  borda: '#E5E7EB',
  
  // Cor do WhatsApp (se usar)
  whatsapp: '#25D366',
  whatsappHover: '#128C7E'
}
```

### Tailwind Config (se usar Tailwind)

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        'primary': '#[HEX_PRIMARIA]',
        'primary-hover': '#[HEX_HOVER]',
        // ... outras cores
      }
    }
  }
}
```

🚦 AVANÇA QUANDO: Cores especificadas
```

---

## 7.6.2 — GERAR ESPECIFICAÇÃO DE TIPOGRAFIA

```
[AUTOMÁTICO] Definir fontes e tamanhos

### Tipografia

#### Fonte Principal

```css
/* Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

/* Ou usar fonte do sistema */
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

#### Escala de Tamanhos

| Elemento | Classe Tailwind | Tamanho | Peso |
|----------|----------------|---------|------|
| Headline principal | text-2xl md:text-3xl | 24px/30px | 700 (bold) |
| Headline secundária | text-xl | 20px | 600 (semibold) |
| Texto corpo | text-base | 16px | 400 (normal) |
| Texto pequeno | text-sm | 14px | 400 (normal) |
| Texto mínimo | text-xs | 12px | 400 (normal) |
| Botão | text-lg | 18px | 600 (semibold) |

🚦 AVANÇA QUANDO: Tipografia especificada
```

---

## 7.6.3 — GERAR ESPECIFICAÇÃO DE COMPONENTES

```
[AUTOMÁTICO] Definir estilos de componentes reutilizáveis

### Componentes Base

#### Botão Primário

```jsx
// Classe Tailwind
className="w-full py-4 px-6 bg-primary text-white rounded-lg font-semibold text-lg
           hover:bg-primary-hover transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed
           active:scale-[0.98]"
```

#### Botão Secundário

```jsx
className="w-full py-3 px-6 bg-white text-primary border-2 border-primary rounded-lg font-semibold
           hover:bg-primary/5 transition-all duration-200"
```

#### Input de Texto

```jsx
className="w-full py-3 px-4 border-2 border-gray-200 rounded-lg
           focus:border-primary focus:ring-2 focus:ring-primary/20
           outline-none transition-all duration-200
           placeholder:text-gray-400"
```

#### Card

```jsx
className="bg-white rounded-2xl p-6 shadow-lg border border-gray-100"
```

#### Botão de Opção (pergunta)

```jsx
// Estado normal
className="w-full py-3 px-4 border-2 border-gray-200 rounded-lg text-left
           hover:border-primary/50 transition-all duration-200"

// Estado selecionado
className="w-full py-3 px-4 border-2 border-primary bg-primary/5 rounded-lg text-left"
```

#### Barra de Progresso

```jsx
// Container
className="w-full h-2 bg-gray-200 rounded-full overflow-hidden"

// Preenchimento
className="h-full bg-primary rounded-full transition-all duration-300"
style={{ width: `${(telaAtual / totalTelas) * 100}%` }}
```

🚦 AVANÇA QUANDO: Componentes especificados
```

---

## 7.6.4 — GERAR ESPECIFICAÇÃO DE RESPONSIVIDADE

```
[AUTOMÁTICO] Definir breakpoints e comportamentos

### Responsividade

#### Breakpoints (Tailwind padrão)

| Prefixo | Largura mínima | Dispositivo |
|---------|----------------|-------------|
| (default) | 0px | Mobile |
| sm | 640px | Mobile grande |
| md | 768px | Tablet |
| lg | 1024px | Desktop |
| xl | 1280px | Desktop grande |

#### Container Principal

```jsx
// Layout mobile-first
className="min-h-screen bg-gray-50 px-4 py-8 
           md:px-8 md:py-12 
           lg:px-0 lg:max-w-lg lg:mx-auto"
```

#### Ajustes por Tela

- **Mobile (< 640px):**
  - Padding lateral: 16px
  - Headlines: text-2xl
  - Botões: py-4
  
- **Tablet (768px+):**
  - Padding lateral: 32px
  - Headlines: text-3xl
  - Botões: py-4
  
- **Desktop (1024px+):**
  - Container centralizado: max-width 512px
  - Padding lateral: 0
  - Headlines: text-3xl

🚦 AVANÇA QUANDO: Responsividade especificada
```

---

## 7.6.5 — GERAR ESPECIFICAÇÃO DE ANIMAÇÕES

```
[AUTOMÁTICO] Definir animações e transições

### Animações

#### Transição entre Telas

```css
/* Fade in */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

.tela-entrada {
  animation: fadeIn 0.3s ease-out;
}
```

#### Loading Spinner

```css
@keyframes spin {
  to { transform: rotate(360deg); }
}

.spinner {
  animation: spin 1s linear infinite;
}
```

#### Barra de Progresso Animada

```css
@keyframes progress {
  from { width: 0; }
  to { width: 100%; }
}

.progress-bar {
  animation: progress 4s ease-out;
}
```

#### Micro-interações

```jsx
// Botão ao clicar
className="active:scale-[0.98] transition-transform"

// Input ao focar
className="focus:ring-2 focus:ring-primary/20 transition-shadow"

// Card ao hover
className="hover:shadow-xl transition-shadow duration-300"
```

🚦 AVANÇA QUANDO: Animações especificadas
```

---

## 7.6.6 — CONSOLIDAR SEÇÃO DE ESTILO

```
[REGISTRO] Juntar todas as especificações de estilo

SEÇÃO 5 DO PROMPT MESTRE:
├── Paleta de cores (com HEX exatos)
├── Tipografia (fonte + escala)
├── Componentes base (classes prontas)
├── Responsividade (breakpoints + ajustes)
└── Animações (keyframes + micro-interações)

🚦 AVANÇA QUANDO: Seção 5 consolidada
```

---

# BLOCO 7.7: VALIDAR E BLINDAR O PROMPT

**Objetivo:** Garantir que o prompt está 100% executável.

---

## 7.7.1 — EXECUTAR CHECKLIST DE COMPLETUDE

```
[AUTOMÁTICO] Verificar se nada está faltando

┌─────────────────────────────────────────────────────────────────────┐
│ CHECKLIST DE COMPLETUDE DO PROMPT MESTRE                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ VISÃO GERAL:                                                       │
│ □ Cabeçalho com instruções ao builder                             │
│ □ Descrição clara do projeto                                      │
│ □ Especificações técnicas (stack)                                 │
│ □ Mapa de navegação                                               │
│ □ Lista de variáveis de ambiente                                  │
│                                                                     │
│ TELAS:                                                             │
│ □ Todas as telas especificadas                                    │
│ □ Cada tela tem ID único                                          │
│ □ Layout em wireframe texto                                       │
│ □ Tabela de elementos com estilos                                 │
│ □ Dados (input/output) claros                                     │
│ □ Comportamentos descritos                                        │
│ □ Navegação (vem de/vai para)                                     │
│                                                                     │
│ LÓGICA:                                                            │
│ □ Função principal de cálculo COMPLETA                            │
│ □ Funções auxiliares                                              │
│ □ Casos de teste                                                  │
│ □ Gerenciamento de estado                                         │
│ □ Validações                                                      │
│ □ Tratamento de erros                                             │
│                                                                     │
│ INTEGRAÇÕES:                                                       │
│ □ Armazenamento (código completo)                                 │
│ □ Webhooks (todos os 5 eventos)                                   │
│ □ WhatsApp (com template de mensagem)                             │
│ □ API de imagem (se modo 1)                                       │
│ □ Curadoria (se modo 2)                                           │
│ □ UTM capture                                                     │
│                                                                     │
│ ESTILO:                                                            │
│ □ Cores em HEX                                                    │
│ □ Tipografia definida                                             │
│ □ Componentes com classes                                         │
│ □ Responsividade                                                  │
│ □ Animações                                                       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

🚦 AVANÇA QUANDO: Todos os itens ✓
```

---

## 7.7.2 — VERIFICAR ZERO AMBIGUIDADE

```
[AUTOMÁTICO] Caçar termos vagos

BUSCAR E ELIMINAR:
❌ "algo como..."
❌ "pode ser..."
❌ "ou similar"
❌ "a definir"
❌ "use sua criatividade"
❌ "escolha uma cor apropriada"
❌ "tamanho adequado"
❌ "algum texto aqui"

SUBSTITUIR POR:
✅ Textos literais entre aspas
✅ Valores exatos
✅ Classes CSS específicas
✅ Código executável

🚦 AVANÇA QUANDO: Nenhum termo vago encontrado
```

---

## 7.7.3 — GERAR SEÇÃO "NÃO FAZER"

```
[AUTOMÁTICO] Criar lista de restrições para o builder

## ⛔ NÃO FAZER

O builder NÃO deve:

### Telas e Layout
- [ ] NÃO adicionar telas não especificadas
- [ ] NÃO remover telas especificadas
- [ ] NÃO alterar ordem das telas
- [ ] NÃO modificar layout das telas

### Textos
- [ ] NÃO alterar nenhum texto (headline, botão, etc.)
- [ ] NÃO corrigir "erros" que pareçam de português
- [ ] NÃO adicionar textos extras

### Lógica
- [ ] NÃO modificar a fórmula de cálculo
- [ ] NÃO adicionar validações extras
- [ ] NÃO alterar fluxo de navegação

### Visual
- [ ] NÃO mudar cores especificadas
- [ ] NÃO usar outras fontes
- [ ] NÃO adicionar animações extras
- [ ] NÃO colocar ícones não especificados

### Funcionalidades
- [ ] NÃO adicionar features "legais" não pedidas
- [ ] NÃO implementar login/cadastro
- [ ] NÃO adicionar analytics próprios (além dos especificados)
- [ ] NÃO conectar APIs não especificadas

### Geral
- [ ] Se algo parecer estranho, MANTER como está
- [ ] Na dúvida, seguir EXATAMENTE o prompt
- [ ] Não tentar "melhorar" o que foi especificado

🚦 AVANÇA QUANDO: Seção "Não fazer" criada
```

---

## 7.7.4 — TESTAR MENTALMENTE O PROMPT

```
[AUTOMÁTICO] Simular execução pelo builder

SIMULAÇÃO MENTAL:

1. Builder lê "Visão Geral"
   □ Entende o que precisa construir?
   □ Sabe qual stack usar?
   
2. Builder lê "Telas"
   □ Consegue criar cada tela sem perguntar?
   □ Sabe exatamente quais elementos colocar?
   □ Sabe os textos exatos?
   
3. Builder lê "Lógica"
   □ Consegue copiar/colar a função?
   □ Sabe onde colocar cada código?
   
4. Builder lê "Integrações"
   □ Sabe quais variáveis de ambiente criar?
   □ Consegue configurar webhooks?
   
5. Builder lê "Estilo"
   □ Sabe as cores exatas?
   □ Consegue aplicar os estilos?

SE QUALQUER RESPOSTA FOR "NÃO":
→ Voltar e detalhar mais

🚦 AVANÇA QUANDO: Simulação passou em tudo
```

---

## 7.7.5 — VERIFICAR CONSISTÊNCIA ENTRE SEÇÕES

```
[AUTOMÁTICO] Garantir que tudo bate

VERIFICAR:

□ Número de telas no mapa = número de telas detalhadas
□ Variáveis no estado = variáveis usadas na lógica
□ Campos no formulário = validações criadas
□ Webhooks na lista = webhooks no código
□ Cores no design = cores usadas nos componentes
□ APIs na referência = APIs no código

SE HOUVER INCONSISTÊNCIA:
→ Corrigir antes de finalizar

🚦 AVANÇA QUANDO: Tudo consistente
```

---

## 7.7.6 — CONSOLIDAR PROMPT MESTRE FINAL

```
[REGISTRO] Gerar documento único

ESTRUTURA FINAL DO PROMPT MESTRE:

1. CABEÇALHO E INSTRUÇÕES
2. VISÃO GERAL DO PROJETO
3. ESPECIFICAÇÕES TÉCNICAS
4. MAPA DE NAVEGAÇÃO
5. VARIÁVEIS DE AMBIENTE
6. TELAS (todas detalhadas)
7. LÓGICA DE NEGÓCIO
8. INTEGRAÇÕES
9. ESTILO VISUAL
10. ⛔ NÃO FAZER

FORMATO: Markdown único
TAMANHO ESTIMADO: 3.000-5.000 palavras
PRONTO PARA: Copiar e colar no Lovable/Replit

🚦 AVANÇA QUANDO: Prompt Mestre consolidado
```

---

# BLOCO 7.8: GERAR MATERIAIS COMPLEMENTARES

**Objetivo:** Criar documentos de apoio para o cliente.

---

## 7.8.1 — GERAR INSTRUÇÕES PARA O CLIENTE

```
[AUTOMÁTICO] Criar documento em linguagem leiga

## 📋 INSTRUÇÕES PARA O CLIENTE

Olá! Para sua isca funcionar perfeitamente, você precisa 
configurar algumas coisas. Não se preocupe, é simples!

### Passo 1: Número do WhatsApp

Você precisa do seu número no formato internacional.

**Exemplo:** Se seu número é (11) 99999-9999
**Formato correto:** 5511999999999

Anote: _______________

---

### Passo 2: Criar conta no Supabase (para salvar leads)
[SE USAR SUPABASE]

1. Acesse: https://supabase.com
2. Clique em "Start your project" (é gratuito)
3. Crie uma conta com seu email
4. Crie um novo projeto (qualquer nome)
5. Vá em: Settings → API
6. Copie:
   - **URL**: algo como `https://xxx.supabase.co`
   - **anon public key**: uma chave longa

Anote:
- URL: _______________
- Key: _______________

---

### Passo 3: Pegar chave da API de Imagem
[SE MODO_IMAGEM = 1]

1. Acesse: [URL_DA_API]
2. Crie uma conta (tem plano gratuito)
3. Vá em: [CAMINHO_MENU]
4. Clique em "Criar nova chave" ou "Generate API Key"
5. Copie a chave gerada

Anote: _______________

---

### Passo 4: Colar as informações no Replit/Lovable

Quando o desenvolvedor (ou você) for usar o prompt:

1. Procure por "Variáveis de Ambiente" ou "Environment Variables"
2. Adicione cada uma:

| Nome da Variável | Valor |
|------------------|-------|
| WHATSAPP_NUMERO | [seu número] |
| SUPABASE_URL | [sua URL] |
| SUPABASE_ANON_KEY | [sua key] |
| [NOME_API]_API_KEY | [sua chave] |

---

### Precisa de ajuda?

Se tiver dificuldade em qualquer passo, me avise!

🚦 AVANÇA QUANDO: Instruções criadas
```

---

## 7.8.2 — GERAR CHECKLIST PÓS-BUILD

```
[AUTOMÁTICO] Criar checklist de verificação

## ✅ CHECKLIST PÓS-BUILD

Depois que o app for construído, verifique:

### Funcionamento Básico
- [ ] App abre sem erro
- [ ] Primeira tela aparece corretamente
- [ ] Headline e textos estão certos

### Fluxo de Perguntas
- [ ] Consigo avançar para cada pergunta
- [ ] Barra de progresso funciona
- [ ] Validações aparecem quando necessário

### Captura de Lead
- [ ] Campos aparecem corretamente
- [ ] Validação de WhatsApp funciona
- [ ] Botão de enviar funciona

### Resultado
- [ ] Tela de loading aparece
- [ ] Textos rotativos funcionam
- [ ] Resultado aparece formatado
- [ ] Meu nome aparece no resultado

### CTA
- [ ] Botão do WhatsApp funciona
- [ ] Mensagem vem pré-preenchida
- [ ] Meus dados estão na mensagem

### Mobile
- [ ] Funciona no celular
- [ ] Textos legíveis
- [ ] Botões clicáveis

### Dados
- [ ] Lead foi salvo no Supabase/Sheets
- [ ] Dados estão corretos

---

### Problemas Comuns e Soluções

**"App não abre"**
→ Verifique se todas as variáveis de ambiente estão preenchidas

**"WhatsApp não abre"**
→ Verifique se o número está no formato correto (5511999999999)

**"Dados não salvam"**
→ Verifique as credenciais do Supabase

**"Imagem não aparece"**
→ Verifique a chave da API de imagem

🚦 AVANÇA QUANDO: Checklist criado
```

---

## 7.8.3 — GERAR RESUMO PARA FASE 8 (MARKETING)

```
[AUTOMÁTICO] Criar briefing para próxima fase

## BRIEFING PARA FASE 8 — MARKETING

### Sobre a Isca

**Nome:** [NOME_DA_ISCA]
**Tipo:** [calculadora | diagnóstico | quiz | etc.]
**Nicho:** [área]
**Público:** [descrição]

### O Que a Isca Faz

[Descrição em 2-3 frases do que acontece quando a pessoa usa]

### Resultado Principal

[O que a pessoa descobre/recebe]

### CTA

[Para onde a pessoa vai depois]

### Features Visuais

**Modo de imagem:** [0 | 1 | 2]
**Descrição:** [o que tem/não tem visualmente]

⚠️ **Importante para Marketing:**
- SE modo 0: Não prometer features visuais elaboradas
- SE modo 1: Pode mencionar [tipo de imagem gerada]
- SE modo 2: Pode mencionar [tipo de imagem mostrada]

### Pontos Fortes para Destacar

1. [Benefício 1]
2. [Benefício 2]
3. [Benefício 3]

### O Que NÃO Prometer

- [Limitação 1]
- [Limitação 2]

🚦 AVANÇA QUANDO: Briefing criado
```

---

## 7.8.4 — GERAR PACOTE DE ENTREGA

```
[AUTOMÁTICO] Organizar todos os arquivos

## PACOTE DE ENTREGA — FASE 7

Arquivos gerados:

```
/entrega_[nome_isca]/
│
├── PROMPT_MESTRE.md
│   → Documento principal para o builder
│   → Copiar e colar no Lovable/Replit
│
├── INSTRUCOES_CLIENTE.md
│   → Passo a passo para configurar
│   → Em linguagem simples
│
├── CHECKLIST_POS_BUILD.md
│   → O que verificar depois de pronto
│   → Problemas comuns e soluções
│
├── BRIEFING_MARKETING.md
│   → Resumo para Fase 8
│   → Features e limitações
│
└── VARIAVEIS_AMBIENTE.md
│   → Lista completa de env vars
│   → Com exemplos de valores
```

🚦 AVANÇA QUANDO: Pacote organizado
```

---

## 7.8.5 — APRESENTAR AO CLIENTE

```
[INTERAÇÃO] Comunicar conclusão

┌─────────────────────────────────────────────────────────────────────┐
│ SCRIPT DE APRESENTAÇÃO:                                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ "🎉 PROMPT MESTRE FINALIZADO!                                     │
│                                                                     │
│ Criei o documento completo para construir sua isca.               │
│                                                                     │
│ O que está incluído:                                               │
│                                                                     │
│ 📄 **PROMPT MESTRE** — A receita completa                         │
│    Isso é o que você vai colar no Lovable ou Replit               │
│    para construir o app automaticamente.                          │
│                                                                     │
│ 📋 **INSTRUÇÕES** — O que você precisa fazer                      │
│    Passo a passo bem simples para configurar                      │
│    WhatsApp, banco de dados, etc.                                 │
│                                                                     │
│ ✅ **CHECKLIST** — Para verificar depois                          │
│    Lista do que testar quando o app estiver pronto.               │
│                                                                     │
│ Próximo passo:                                                     │
│ Vou gerar os materiais de MARKETING (anúncios, posts,            │
│ headlines) para você divulgar sua isca!                           │
│                                                                     │
│ Quer que eu continue para o Marketing?"                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

🚦 AVANÇA QUANDO: Apresentação feita
```

---

## 7.8.6 — REGISTRAR E INICIAR PRÓXIMA FASE

```
[REGISTRO] Salvar conclusão e avançar

{
  "fase": "PROMPT_MESTRE",
  "status": "COMPLETA",
  
  "prompt_mestre": {
    "gerado": true,
    "tamanho_palavras": [X],
    "numero_telas": [Y],
    "tem_api_imagem": [true/false],
    "modo_imagem": [0/1/2/3]
  },
  
  "materiais_complementares": {
    "instrucoes_cliente": true,
    "checklist_pos_build": true,
    "briefing_marketing": true,
    "variaveis_ambiente": true
  },
  
  "arquivos_gerados": [
    "PROMPT_MESTRE.md",
    "INSTRUCOES_CLIENTE.md",
    "CHECKLIST_POS_BUILD.md",
    "BRIEFING_MARKETING.md",
    "VARIAVEIS_AMBIENTE.md"
  ],
  
  "cliente_informado": true
}

🚦 AVANÇA QUANDO: → INICIAR FASE 8 (MARKETING)
```

---

# 📊 RESUMO FINAL — FASE 7: PROMPT MESTRE

## CONTAGEM DE MICRO-TAREFAS

| Bloco | Nome | Tarefas |
|-------|------|---------|
| 7.1 | Carregar e Consolidar Entradas | 6 |
| 7.2 | Montar Seção Visão Geral | 6 |
| 7.3 | Montar Seção Telas | 10 |
| 7.4 | Montar Seção Lógica | 8 |
| 7.5 | Montar Seção Integrações | 10 |
| 7.6 | Montar Seção Estilo | 6 |
| 7.7 | Validar e Blindar | 6 |
| 7.8 | Gerar Materiais Complementares | 6 |
| **TOTAL** | | **58 micro-tarefas** |

---

## MATERIAIS DE APOIO NECESSÁRIOS

| Material | Prioridade |
|----------|------------|
| Blueprint da Fase 6 | CRÍTICA |
| Mapa de Telas da Fase 6 | CRÍTICA |
| Copy do Branding | CRÍTICA |
| Oferta da Fase 5 | CRÍTICA |
| `APIS_POR_FUNCAO.md` | ALTA |
| `TEMPLATE_PROMPT_MESTRE.md` | ALTA |
| `EXEMPLOS_PROMPTS_SUCESSO.md` | MÉDIA |

---

## CHECKLIST DE SAÍDA — PROMPT MESTRE

```
OBRIGATÓRIO (não avança sem):
□ Prompt Mestre gerado (documento único)
□ Todas as telas especificadas com detalhes
□ Lógica de cálculo em código executável
□ Integrações com código pronto
□ Estilo visual com valores exatos
□ Seção "Não Fazer" incluída
□ Zero ambiguidade verificada
□ Consistência entre seções verificada
□ Instruções para cliente criadas
□ Checklist pós-build criado
□ Briefing para Marketing criado

CONDICIONAL:
□ Integração API de imagem (se modo 1)
□ Lógica de curadoria (se modo 2)
□ Integração Calendly (se CTA agendamento)

ARQUIVOS GERADOS:
□ PROMPT_MESTRE.md
□ INSTRUCOES_CLIENTE.md
□ CHECKLIST_POS_BUILD.md
□ BRIEFING_MARKETING.md
□ VARIAVEIS_AMBIENTE.md
```

---

## A TRANSFORMAÇÃO QUE ESTA FASE PRODUZ

```
ANTES DO PROMPT MESTRE:
• Temos blueprint técnico
• Temos especificações separadas
• Temos copy definida
• MAS está espalhado em vários lugares
• E pode ter ambiguidades

DEPOIS DO PROMPT MESTRE:
✅ DOCUMENTO ÚNICO pronto para copiar/colar
✅ ZERO AMBIGUIDADE em cada elemento
✅ CÓDIGO EXECUTÁVEL de toda a lógica
✅ INTEGRAÇÕES prontas para usar
✅ INSTRUÇÕES para cliente em linguagem simples
✅ CHECKLIST para verificar pós-build

→ Builder (Lovable/Replit) EXECUTA SEM ERRAR
→ Cliente CONFIGURA SEM DÚVIDAS
→ App FUNCIONA na primeira tentativa
```

---

## PRINCÍPIO FUNDAMENTAL

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│  "O Prompt Mestre é a TRADUÇÃO PERFEITA."                         │
│                                                                     │
│  Ele pega TODA a inteligência das 6 fases anteriores:             │
│  • Discovery (negócio, público)                                    │
│  • Psychology (desejos, objeções)                                 │
│  • Ideation (tipo de isca, mecânica)                              │
│  • Branding (copy, tom de voz)                                    │
│  • Oferta (CTA, promessa, garantia)                               │
│  • Engenharia (telas, lógica, integrações)                        │
│                                                                     │
│  E transforma em UM DOCUMENTO que:                                 │
│  • Qualquer builder entende                                        │
│  • Qualquer ferramenta executa                                    │
│  • Nenhum humano precisa interpretar                              │
│                                                                     │
│  REGRA DE OURO:                                                    │
│  "Se o builder precisar tomar UMA decisão sozinho,                │
│   o Prompt Mestre está INCOMPLETO."                               │
│                                                                     │
│  O resultado é um app que:                                         │
│  ✅ Funciona na primeira tentativa                                │
│  ✅ Converte visitantes em leads                                  │
│  ✅ Leva leads para o CTA do cliente                              │
│  ✅ Já nasce pronto para escalar                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

# FIM DA PARTE 7 — PROMPT MESTRE

**Próxima parte:** FASE 8: MARKETING (Divulgação da Isca)

---

**Versão:** 1.0 Definitiva  
**Formato:** Híbrido Executável  
**Status:** 100/100 — Aprovada para implementação
